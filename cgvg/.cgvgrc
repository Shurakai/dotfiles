# This file configures cgvg on a per-user basis.
SEARCH=(.*$)

# Exclude files that I don't need.
EXCLUDE=^(c)?tags$|.*\.(out|js|pdf)$|.*doc/.*\.(tag|html)$|(build|depend)\.make|testlist|CMakeFiles/.*|\.git/.*|build/.*|Makefile

COLOR2=yellow
