# This file configures cgvg on a per-user basis.
SEARCH=(.*$)

# Exclude files that I don't need.
EXCLUDE=^(c)?tags$|.*\.out$|.*doc/.*\.(tag|html)$|.*\.js|(build|depend)\.make|testlist|CMakeFiles/.*|\.git/.*
